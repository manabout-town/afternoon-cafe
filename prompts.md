# 지시문 모음

## 한 번에 고치기

```
이 프로젝트의 화면을 아래 10개 규칙에 맞게 고쳐 줘.
한 번에 하나씩 순서대로 진행하고, 규칙마다 ① 지금 값 ② 바꾼 값 ③ 바꾼 파일을 표로 보고해 줘.
기능·문구·레이아웃 구조는 바꾸지 말고 스타일 값만 바꿔.

1. 배경 #FFFFFF·#000000 → 라이트 #F6F5F2 / 다크 #0B0C0E, 카드는 한 단계 밝게
2. 본문 14px, 입력칸만 16px
3. 줄 간격 본문 1.6, 큰 제목 1.1, 중간 제목 1.2
4. 자간 본문 -0.011em, 큰 제목 -0.035em, 중간 제목 -0.025em
5. 폰트 Pretendard (CDN 링크 추가), 가격·라벨은 IBM Plex Mono
6. 글자색은 같은 먹색의 투명도 4단계(.92/.68/.60/.38)로 통일(단, .38은 비활성·장식 요소 전용, 정보 텍스트 금지), 그라디언트 버튼은 단색으로
7. 모서리는 4/6/8px 한 계열로 통일
8. gap 16px 일괄값을 안쪽 6~12px, 섹션 사이 32px로
9. transition all·ease 를 160ms cubic-bezier(0,0,.2,1)로, 카드처럼 큰 동작만 480ms cubic-bezier(.16,1,.3,1)
10. 전체 폭 1244px, 긴 문단 max-width 36em

마지막으로 이모지 아이콘과 "특별한 경험을 선사합니다" 같은 빈 문구가 있으면 목록만 보여 줘. 고치지는 마.
```

## 장별 지시문

### 1 배경
```
배경색을 순백 #FFFFFF 대신 #F6F5F2로 바꾸고, 카드 배경은 #FBFAF8로 해 줘. 다른 곳은 건드리지 마.
```
### 2 글자 크기
```
본문 글자를 16px에서 14px로, 작은 안내 글씨는 12px로 바꿔 줘. 입력칸이 있으면 입력칸만 16px로 둬.
```
### 3 줄 간격
```
줄 간격을 본문 1.6, 큰 제목(h1) 1.1, 중간 제목(h2) 1.2로 바꿔 줘. 전체에 1.5를 한꺼번에 준 코드는 지워 줘.
```
### 4 자간
```
자간을 본문 -0.011em, h1 -0.035em, h2 -0.025em으로 바꿔 줘. 작은 영문 라벨만 11px, +0.05em, 대문자로.
```
### 5 폰트
```
Pretendard 웹폰트를 CDN으로 불러와 본문 폰트로 쓰고, 가격·라벨에는 IBM Plex Mono를 써 줘. 시스템 폰트는 예비용으로 뒤에만 남겨.
```
### 6 글자색
```
글자색을 회색 여러 개 대신 rgba(26,24,20, 투명도) 하나로 통일해 줘. 제목·본문 강조 .92, 설명 .68, 작은 안내 .60. .38은 비활성·장식 요소에만 쓰고 정보 텍스트에는 쓰지 마. 그라디언트 버튼은 먹색 단색 버튼으로, 이모지 아이콘은 지워 줘.
```
### 7 모서리
```
모서리 둥글기를 한 계열로 통일해 줘. 이미지 4px, 버튼 6px, 카드와 지도 8px.
```
### 8 간격
```
gap 16px 일괄값을 바꿔 줘. 버튼 사이 8px, 메뉴 카드 사이 12px, 카드 안쪽 6px, 지도와 안내 사이 32px.
```
### 9 움직임
```
transition: all .3s ease 를 지우고, 버튼은 배경색·위치만 160ms cubic-bezier(0,0,.2,1), 카드는 위치만 480ms cubic-bezier(.16,1,.3,1)로 바꿔 줘. 움직임 줄이기 설정을 켠 사람에게는 움직임을 끄고.
```
### 10 폭
```
전체 폭을 1244px로, 긴 문단은 max-width 36em으로 제한해 줘.
```

## 그 밖의 지시문

책의 첫 10분 · PART 1 · PART 3에 나오는 지시문입니다. 대괄호 [ ] 부분만 내 상황으로 바꿔 보내세요.

### 첫 10분 — 12줄 붙여 달라고 하기
```
이 HTML 파일의 </head> 바로 위에 아래 코드를 그대로 붙여 줘.
다른 곳은 한 글자도 바꾸지 마. 붙인 뒤 바뀐 부분만 보여 줘.
(여기에 12줄 붙여넣기)
```

### PART 1 — 스타일 주문에 꼭 붙이는 두 문장
```
기능·문구·배치 구조는 바꾸지 말고 스타일(색·글꼴·모서리·여백)만 바꿔 줘.
다른 스타일 요소(그라디언트, 이모지 아이콘, 반짝이 효과)는 섞지 마.
```

### PART 1 — 스타일 주문서
```
이 페이지를 [스타일 이름] 스타일로 바꿔 줘.
- 배경: [밝은/어두운], [색 또는 느낌]
- 주요 요소: [버튼 모양], [카드 모서리], [제목 글꼴]
기능·문구·배치 구조는 바꾸지 말고 스타일(색·글꼴·모서리·여백)만 바꿔 줘.
다른 스타일 요소(그라디언트, 이모지 아이콘, 반짝이 효과)는 섞지 마.
바꾼 뒤 무엇을 바꿨는지 표로 알려 줘.
```

### PART 3 — 빈 문구를 사실로 다시 쓰기
```
아래 사실만 써서 첫 화면 제목 한 줄과 소개 두 문장을 다시 써 줘.
- [사실 1: 예) 화요일과 금요일에 원두를 볶는다]
- [사실 2: 예) 창가 자리 여섯 개, 예약 없이 먼저 온 순서]
- [사실 3]
"특별한", "프리미엄", "최고의", "경험", "선사" 같은 말은 쓰지 마.
사실 목록에 없는 내용은 지어내지 마. 후보를 세 개 보여 줘.
```

### PART 3 — 색·모서리를 변수로 모으기
```
이 프로젝트에 직접 적힌 배경색·카드색·글자색·모서리 값을 :root 변수로 모아 줘.
이름은 --bg, --surface, --ink(r,g,b 숫자만), --on-ink, --r-sm, --r-md, --r-lg로 하고,
글자색은 rgba(var(--ink), 투명도) 모양으로 바꿔 줘. 화면 모습은 바뀌지 않아야 해.
바꾼 뒤 변수 목록과 원래 값을 표로 보여 줘.
```

### PART 3 — Next.js 폰트 연결 + Tailwind 클래스 바꾸기
```
이 Next.js 프로젝트에 Pretendard(본문)와 IBM Plex Mono(가격·숫자)를 연결해 줘.
프로젝트가 이미 쓰는 방식이 있으면 그 방식을 따르고, 없으면 가장 단순한 방법으로.
그다음 아래 표대로 Tailwind 클래스를 바꿔 줘. 기능·문구·배치 구조는 바꾸지 마.
(여기에 책 3.5 변환표에서 필요한 줄 붙여넣기)
```
